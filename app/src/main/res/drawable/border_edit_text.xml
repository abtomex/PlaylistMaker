<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/border_edit_text.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Сплошная обводка (как на макете) -->
    <stroke
        android:width="1dp"
        android:color="@color/new_playlist_inputs_stroke" />

    <!-- Белая заливка внутри -->
    <solid android:color="@color/new_playlist_background" />

    <!-- Небольшое скругление углов (опционально, для мягкости) -->
    <corners android:radius="4dp" />
</shape>